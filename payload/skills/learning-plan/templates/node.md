# NN — <Node Title>

## Prereqs
- <prereq node id or external concept>

## Enables
- <downstream node id>

## What this node is
<1–3 sentences. What concept does this node cover, and what role does it play in the module's DAG? If it's a fluency-heavy node, say so. If it's a schema-building peak, say so.>

## Sources
- <Specific resource — Khan Academy section URL, textbook chapter, YouTube video. Avoid vague references.>
- <Another source if helpful>

## Fluency exercises
Goal: automaticity. The user should do these without conscious effort by the end of the fluency session.

1. <problem>
2. <problem>
3. <problem>
4. <problem>
5. <problem>

> If this is a schema-heavy node where fluency exercises don't make sense, replace this section with: `Fluency-light node — schema only. See exercises below.`

## Schema-building exercises
Goal: force connection, derivation, or transfer. These are the flight (philosophy §6).

1. **<short title>.** <prompt that requires explanation, derivation, or novel application>
2. **<short title>.** <prompt>
3. **<short title>.** <prompt>

## Metacog checks
Run these before advancing. Minimum bar to advance: pass the explanation check and at least one of transfer or connection.

- **Explanation**: <question about how/why this concept works>
- **Transfer**: <question requiring use in a context not covered above>
- **Connection**: <question linking this node to ≥2 others by name>

## Notes (optional)
<Anything else: known stumbling blocks, where the philosophy applies most directly, why this node matters in the larger DAG.>
